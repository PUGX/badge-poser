language: php

sudo: false

php:
  - 5.6
  - 7.0
  - 7.1

services:
  - redis-server

before_script:
  - phpenv config-rm xdebug.ini
  - composer install

notifications:
  email:
    - liuggio@gmail.com
